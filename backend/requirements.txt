# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3      # driver Postgres (sync) per Neon
alembic==1.14.0

# --- External APIs ---
# Nessun SDK: wger, USDA FoodData Central, TheMealDB e Google Gemini vengono
# tutti chiamati via REST con httpx.

# --- Utils ---
python-dotenv==1.2.3
httpx==0.28.1

# --- Test ---
pytest==9.1.1

# --- Autenticazione ---
# pwdlib con Argon2: algoritmo raccomandato da OWASP per le password, senza
# i limiti di bcrypt sulla lunghezza. PyJWT per i token di sessione.
pwdlib[argon2]==0.2.1
pyjwt==2.14.0
python-multipart==0.0.32

# --- Notifiche push ---
# Web Push con chiavi VAPID: gratuito, senza account presso Google o Apple.
pywebpush==2.5.0
